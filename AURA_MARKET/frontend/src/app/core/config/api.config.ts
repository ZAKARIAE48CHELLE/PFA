export const API_CONFIG = {
  // When working locally, use localhost:8080
  // When using ngrok, replace this with your ngrok backend URL (e.g., https://your-backend.ngrok-free.dev)
  baseUrl: 'http://localhost:8080'
};
